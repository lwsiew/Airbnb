<% @listing.each do |listing| %>
	<%= link_to listing.name, admin_index_path(listing) %><br/>
  		<%= listing.description %><br/>
  		<%= listing.country %>
  		<%= listing.state %>
  		<%= listing.city %><br/>
  		Price : <%= listing.price %> <br/>
<% end %>